# Copy this file to secrets.properties. The real file is ignored by Git.
# Restrict the key in Google Cloud to Android app com.feastly.app and the
# production Play App Signing SHA-1 before enabling the Maps SDK for Android.
SAVRIVO_MAPS_ANDROID_API_KEY=replace_with_restricted_android_maps_key
